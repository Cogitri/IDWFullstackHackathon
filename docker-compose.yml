version: "3.7"

services:
  database:
    image: mariadb:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
  backend:
    container_name: backend
    build: ./backend
    ports:
      - 8000:8000
    restart: always
